# Copy to .env.local for local development (never commit .env.local)

# Required in production (Netlify → Site settings → Environment variables)
AUTH_SECRET=replace-with-a-long-random-string-at-least-32-chars
ADMIN_USERNAME=admin
ADMIN_PASSWORD=replace-with-a-strong-password

# Local SQLite (default). On Netlify serverless, file storage does NOT persist —
# use Turso instead (see docs/DEPLOY-NETLIFY.md).
DATABASE_PATH=data/tile-logistics.db
UPLOAD_ROOT=data/uploads

# Turso (hosted SQLite) — set these on Netlify for persistent production data
# TURSO_DATABASE_URL=libsql://your-db-name-org.turso.io
# TURSO_AUTH_TOKEN=your-token
